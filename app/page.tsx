export default function Home() {
  return (
    <main style={{padding: 40}}>
      <h1>Portfolio Build Check</h1>
      <p>If you can see this after running `npm run build`, the build pipeline works. The issue is in the original page content.</p>
    </main>
  );
}
